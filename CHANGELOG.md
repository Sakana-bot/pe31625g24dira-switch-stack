# Changelog

## v1.3.2 — 2026-08-31

- SDK/TestPoint 调度改为单一优先级队列；后台传感器、光引擎缓存、环路保护和 MAC 核对任务自动合并，不再绕过队列抢锁。
- 光引擎身份与 RX 功率缓存不再重复读取温度；温度统一由周期传感器采样提供。
- 收紧 deploy-kit 文件白名单，拒绝 Python 缓存、内部构建工具和开发交接资料进入用户包。
- 修正桌面端移动导航按钮的 CSS 优先级，并为本地预览补齐光引擎数据。
- 通用部署实现改用中性 core 脚本名，旧 Debian 13 文件名仅作为兼容入口保留。
- 将单体前端脚本按实时概览、硬件诊断和系统维护拆分为原生 ES modules，`app.js` 仅保留页面编排与交换配置主流程。
- 光引擎温度单次读取失败时自动重试并沿用最近有效值，避免界面瞬时缺失；系统设置新增 IANA 时区配置。
- 光引擎身份只读取到部分分支时保留有效数据并按 30 秒至 5 分钟退避重试，不再把不完整采样永久缓存为完成。

## v1.3.1 — 2026-08-31

- 自动读取并缓存两块板载 FCI/Amphenol 光引擎的身份与内部温度，在概览和板卡传感器中持续更新。
- 明确区分光引擎内部温度与不可用的 RX 光功率，不再要求用户手动执行完整诊断。
- 合并 FM10840 与光引擎监控面板，统一采样时间、刷新入口及高密度列表样式。
- SDK/TestPoint 操作改为有限 FIFO 队列，繁忙时自动等待，不再直接返回操作冲突。

## v1.3.0 — 2026-08-28

- 同一套安装源支持 Debian 与 Ubuntu，并在 Ubuntu 26.04 / Linux 7.0 实板完成本机 DKMS 编译和整机部署验证。
- 保留发行版已有网络管理方式，仅为第二管理口增加项目独立的 `192.168.255.2/24` 配置。
- fm10k timer 兼容层按目标内核提供的 API 自动选择，不再硬编码发行版或内核版本。
- 清理未参与当前运行的历史包依赖、迁移补丁和来源板卡元数据副本。
- 移动端改用可关闭的侧栏抽屉，保持桌面端信息架构并避免页面横向溢出。
- 在线更新支持选择预发布版本，并以一次性确认替代常驻降级开关。
- 修正移动端变更预览角标和活动端口速率标签的布局。

## v1.2.0 — 2026-08-28

- 全新安装直接生成固定 24 槽位逻辑端口模型，消除首次端口拓扑修改所需的整服务迁移重启。
- 系统信息按语义重新归类：CPU 与 BIOS 归入设备，根文件系统用量明确显示为系统盘。
- 光通道诊断按 SDK 的真实返回值显示眼高；眼宽为 `0xFF` 不可用时不再显示误导性的 `NA` 评分。
- 重写 WebUI 与项目交接文档，并将原系统镜像分析明确标记为历史研究资料。
- 概览与系统信息统一使用“系统盘”，并清理已停用主题方案的说明残留。
- 一键安装从最新 Release 获取管理软件、从首次正式发布获取稳定 runtime，后续 Release 不再重复上传相同 SDK 包。

## v1.1.0 — 2026-08-27

- 管理软件与 deploy-kit 改用仓库根目录的单一版本号。
- 页面按“状态 / 交换 / 系统”重组，传感器、散热与系统信息职责分离。
- 系统信息新增根分区占用及运行环境中的 IES SDK、TestPoint、fm10k UIO/IES 驱动版本。
- 交换服务状态改为综合检查 systemd、UIO 和 TestPoint 就绪标记。
- 驱动版本纠正为 `6.12.101-ies1`，并自动迁移旧的 DKMS 包装号。

## v1.0.0 — 2026-08-26

首个正式版本。

> 本项目全部代码由 ChatGPT 生成，不保证正确性、可靠性、安全性、可用性或适用于任何用途。

- WebUI 1.0.0：端口、VLAN、硬件监控、日志、备份恢复及 GitHub 在线升级。
- deploy-kit 1.0.0：首次安装、审计、完整卸载和保留配置的增量升级。
- legacy SDK runtime 2.1.0：从原始系统盘提取并清除设备身份信息，适配 `sil001-hw4-b0`。
- deploy-kit 内置已验证 platform，首次安装按需配合同一 Release 的 runtime；未知 platform 默认拒绝部署。
- 在线和上传更新包会自动校验 SHA-256、比较版本、审计差异并在确认后执行；驱动通过 DKMS 针对当前内核现场构建。
