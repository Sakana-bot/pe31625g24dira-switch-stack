# Switch Manager 界面规范

本规范用于约束所有页面的导航、字号、字重和文字颜色。新增页面必须复用这些语义层级，不得按局部观感临时覆盖。

## 信息架构

- 一级入口只表示功能域：`状态`、`交换`、`系统`。
- 二级入口才对应可访问页面：
  - 状态：概览、传感器、日志
  - 交换：端口、VLAN、网络功能
  - 系统：系统信息、散热、备份与升级、设置
- 一级入口使用实色紫色表示当前功能域；二级入口使用底部强调线表示当前页面。悬停和键盘聚焦时强调线从左向右展开，移开后收回。二级当前项只改变字重，文字保持对应主题的主文字色，不染成紫色。
- 侧栏按明暗模式分别设计：浅色模式使用浅灰白底和深色文字，深色模式使用深灰底和浅色文字；两者共用紫色一级选中态，但边界、悬停、辅助文字和二级当前态不得共用硬编码颜色。
- 统计是端口页的二级视图，不再占用侧栏入口。
- 小于等于 700px 时，侧栏变为由顶栏菜单按钮打开的抽屉；选择页面、点击遮罩或按 Esc 后关闭。移动端不得把全部二级入口平铺成一条横向滚动栏。
- 数据表可在自身容器内横向滚动，但页面根节点不得出现横向溢出；表单、操作栏和多列卡片在移动端改为单列。

## 字号层级

| 层级 | 字号 | 用途 |
| --- | --- | --- |
| 页面标题 | 20px | 顶栏当前页面 |
| 弹窗标题 | 17px | 确认和编辑弹窗 |
| 卡片标题 | 16px | 系统、管理口、RMON 等面板 |
| 分组标题 | 14px | 接收、发送、EPL 分组、对象名称 |
| 正文 | 13px | 表头、字段、表单、常规数值 |
| 辅助信息 | 12px | 物理路径、时间、单位、说明 |
| 微型标注 | 11px | 输入范围等必须保留的短标注 |

## 颜色与字重

- `主文字色 + 600`：页面/卡片/分组标题、当前对象名称、活动端口名称、重要 KPI。
- `主文字色 + 500`：指标名称（速率、单播、错误字节）和确需强调的表单字段名。
- `主文字色 + 400`：Lane、聚合端口、端口自定义名称、传感器条目、风扇转速、系统详情值、表格正文、普通配置内容和长文本。
- `次级文字色 + 400`：物理路径、EPL 编号、单位、时间、输入范围、说明文案，以及键值列表中的键名。
- 状态文字只使用状态组件的绿色、橙色或红色；不得用加粗代替状态色。
- 表头统一使用主文字色、13px、600，不使用灰色底或更小字号。
- 不把所有数值加粗。只有 KPI、状态计数和需要快速扫描的统计值使用 600。

## 二级说明文案

- 二级说明不是卡片标题的必备组成；默认不写，只有能补充统计范围、数据来源、计算口径、操作影响或风险时才保留。
- 不得换一种说法重复标题，也不得描述用户从控件本身即可理解的动作。例如 `CPU 使用率 / 处理器整体负载`、`内存使用率 / 系统内存占用`、`监控与统计 / 控制历史趋势和端口流量的本地保存` 均应只保留标题。
- 有效说明应提供标题中没有的信息。例如交换速率使用 `全部逻辑端口汇总`，每日流量使用 `最近 30 天收发流量`，端口排行使用 `按今日收发总量排序`。
- 文案使用辅助信息层级：12px、次级文字色、400 字重；控制在一行短语内，不使用加粗、状态色或无意义句号。
- 空状态、错误、等待采样和风险提示属于状态反馈，不得为了减少二级说明而删除，也不得塞进标题下方作为常驻说明。
- 新增或调整卡片时，评审顺序为：先删除说明检查标题是否已经自明，再判断是否确有范围或口径需要补充。

## 图标

- 通用操作图标使用 Lucide 风格的 24×24 线性 SVG，`fill:none`、圆角端点和连接。
- 同一动作只能使用一个图形语义。电源入口使用 `Power`，重启使用 `RotateCcw`，关机使用 `PowerOff`。
- 图标不替代文字标签；只有顶栏电源入口可在具有 `aria-label` 和 `title` 时单独显示。

## 浮层层级

- 页面内容、顶栏和侧栏位于基础层；模态遮罩统一使用 `--z-modal`。
- 下拉菜单使用 `--z-dropdown`，确保弹窗内的选择器可以正常展开。
- 全局任务进度使用 `--z-progress`，Toast 使用最高的 `--z-toast`；两者不得被任何模态遮罩或模糊效果覆盖。
- 新增浮层必须复用现有层级变量，不得在局部随意增加更大的 `z-index`。
